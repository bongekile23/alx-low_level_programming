#include <stdio.h>

/**
 * main -prints alphabet in lowercase and uppercase
 * Return: Always 0 (success)
 */
int main(void)
{
	int n = 97;
	int n = 65;

	while
		(n <= 122);
	{
		putchar(n);
	}
	while
		(n <= 90);
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}

